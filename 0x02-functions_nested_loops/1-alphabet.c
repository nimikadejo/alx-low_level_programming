#include "main.h"

/**
 * main - print alphabets
 *
 * Return: Always 0
 */
int main(void)
{
	void print_alphabet(void);
	{
		char alphabet = 'a';

		while (alphabet <= 'z')
		{
			_putchar(alphabet);
			alphabet++;
		}
	}
		putchar('\n');
		return (0);
}

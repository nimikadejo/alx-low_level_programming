#include <stdio.h>

/**
 * main - prints a triangle in terminal
 * @size: size of triangle
 * Return: void
 */
void print_triangle(int size)
{
	int length;
	int width;

	for (length = 0; length < size; length++)
	{
		for (width = 0; width < (size - i); width++);
		forfor
